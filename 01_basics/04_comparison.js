// console.log(2 > 1);
// console.log(2 >= 1);
// console.log(2 < 1);
// console.log(2 == 1);
// console.log(2 != 1);


// console.log("2" > 1);
// console.log("02" > 1);

console.log(null > 0);
console.log(null == 0);
console.log(null >= 0);

console.log(undefined == 0);
console.log(undefined > 0);
console.log(undefined < 0);

// === 

console.log("2" === 2);

//chatgpt practice question of comparison

// Compare the numbers 5 and 8. Store the result in a variable called 'result'.
let result = 5 > 8;


// Compare the strings "apple" and "orange". Store the result in a variable called 'result'.
let results = "apple" === "orange"

// Compare the boolean values true and false. Store the result in a variable called 'result'.
let resul = true === false;
// and
let resut = true == false;

// Check if the numbers 10 and 10 are equal. Store the result in a variable called 'result'.
let reult = 10 === 10;
//and
let reslt = 10 == 10;

// Check if the boolean value true is truthy. Store the result in a variable called 'result'.
let truthy = 1;
let rest = truthy === 1;

// Check if the values null and undefined are equal. Store the result in a variable called 'result'.
let resu = null ===undefined 


// Compare the string "5" with the number 5 using loose equality (==). Store the result in a variable called 'result'.
let rlt = ("5" == 5)

// Check if the string "5" is strictly equal to the number 5. Store the result in a variable called 'result'.
let res= ("5" === 5)

// Check if positive Infinity is greater than 100. Store the result in a variable called 'result'.
let rsult = Infinity > 100;

// Check if the result of dividing the string "abc" by 2 is NaN. Store the result in a variable called 'result'.
let resrlt = isNaN("abc" / 2);
